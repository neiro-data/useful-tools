"""Root API router stub.

Feature-specific routers (projects, entries, reports, etc.) should be created in their own
modules and included here as the app grows. Kept empty in this scaffold on purpose.
"""

from fastapi import APIRouter

router = APIRouter()
